Import('RTT_ROOT')
Import('rtconfig')
from building import *

objs = []
cwd     = GetCurrentDir()
list = os.listdir(cwd)
src     = Glob('*.c')
CPPPATH = [cwd, str(Dir('#'))]

group = DefineGroup('ulog_back', src, depend = ['PKG_USING_ULOG_BACK'], CPPPATH = CPPPATH)

Return('group')
